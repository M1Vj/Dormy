# Active Protocol: 05 — Fines System

## 0. Dispatch Plan (For Orchestrator)
- **Backend Coder**: YES (Server Actions)
- **Frontend Coder**: YES (UI Components + Page)
- **Order**: Backend -> Frontend
- **Key Dependencies**: Supabase Types, Shadcn UI, `actions/occupants` (re-use)

## 1. Objective
Implement the Fines module: Rules management, Fine Issuance, and Centralized Ledger.

## 2. Implementation Specs
- **Plan File**: `.maba/plans/05-fines.md`
- **Context**: We are building on top of the existing Supabase schema (`fines`, `fine_rules`).

## 3. Backend Spec (For Backend Coder)
- **File**: `src/app/actions/fines.ts`
- **Scope**:
  - `getFineRules`, `createFineRule`, `updateFineRule`
  - `getFines` (with filters: dormId, occupantId?)
  - `issueFine` (transactional insert)
  - `voidFine` (soft delete logic)
- **Validation**: Ensure `dorm_id` matches the session.

## 4. Frontend Spec (For Frontend Coder)
- **Page**: `src/app/(app)/admin/fines/page.tsx` (Tabs: Ledger | Rules)
- **Dialogs**:
  - `CreateRuleDialog`: Simple CRUD form.
  - `IssueFineDialog`: Complex form. Needs `OccupantCombobox` (reusable or new) + Rule selection that auto-fills defaults.
- **Integration**:
  - Add "Issue Fine" button to `src/app/(app)/admin/occupants/[id]/page.tsx`.

## 5. Step-by-Step Instructions (The Prompt)
1. **[Backend]**: Read `.maba/plans/05-fines.md` section 3. Implement `src/app/actions/fines.ts`.
2. **[Frontend]**: Read `.maba/plans/05-fines.md` section 4. Implement components in `src/components/admin/fines/`.
3. **[Frontend]**: Assemble `src/app/(app)/admin/fines/page.tsx`.
4. **[Integration]**: Add button to Occupant Profile.

## 6. Definition of Done
- `npm run lint` passes.
- Admin can define rules.
- Admin can issue a fine from the global page OR the occupant profile.
- **[Backend Coder]**: Implemented `src/app/actions/fines.ts` with Rule CRUD and Fine Issuance/Void actions.
- **[Frontend Coder]**: Built Fines UI: Page (Tabs), Rules Table (CRUD), Ledger (View/Filter), Issue Dialog (Sheet with Search). Added 'Issue Fine' to Occupant Profile. Fixed lint errors.
- **[Frontend Coder]**: Updated `page.tsx` (next 15 params), `occupants.ts` (types), `auth-provider.tsx` (lint) to fix Reviewer findings.
- **[Reviewer]**: Re-verified fixes. All clear. (PASS)
- **[Orchestrator]**: Delegating to **Logger** to finalize "05-fines".

## 7. Journal
[Reviewer]: Checking feature 05-fines...
[Reviewer]: PASSED - Backend actions (`src/app/actions/fines.ts`) implemented correctly with Zod validation and RLS checks.
[Reviewer]: PASSED - Frontend components (`FinesLedger`, `IssueFineDialog`, `RulesTable`) structure and logic are sound.
[Reviewer]: FOUND ISSUE - `src/app/(app)/admin/occupants/[id]/page.tsx` uses `params.id` synchronously. In Next.js 16, `params` is a Promise and must be awaited.
[Reviewer]: FOUND ISSUE - `npm run lint` failed. `src/app/actions/occupants.ts` has multiple `any` type errors. `auth-provider.tsx` has a critical `set-state-in-effect` error.
[Reviewer]: FAILED. Fix `params` usage in Occupant Profile and resolve Lint errors.
