import Page from "@/app/(app)/occupant/evaluation/page";

export default function RoleEvaluationPage(props: { searchParams?: Promise<unknown>, params?: Promise<unknown> }) {
  return <Page {...props} />;
}
