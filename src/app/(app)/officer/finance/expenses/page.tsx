import Page from "@/app/(app)/admin/finance/expenses/page";

export default function ReExportedPage(props: { searchParams?: Promise<unknown>, params?: Promise<unknown> }) {
  return <Page {...props} />;
}
