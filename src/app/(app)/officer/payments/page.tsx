import Page from "@/app/(app)/occupant/payments/page";

export default function RolePaymentsPage(props: { searchParams?: Promise<unknown>, params?: Promise<unknown> }) {
  return <Page {...props} />;
}
