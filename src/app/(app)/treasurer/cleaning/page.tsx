import Page from "@/app/(app)/occupant/cleaning/page";

export default function RoleCleaningPage(props: { searchParams?: Promise<unknown>, params?: Promise<unknown> }) {
  return <Page {...props} />;
}
